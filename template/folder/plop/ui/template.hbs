import * as PIXI from 'pixi.js'

type render = () => void

type {{ pascalCase name }}Options = {}

const create{{ properCase name }} = ({}: {{ pascalCase name }}Options): [PIXI.Container, render] => {
  const container = new PIXI.Container()

  const render = () => {}

  return [container, render]
}

export default create{{ properCase name }}
