import * as pixi from '/pixi'
import { SceneArgs, RenderScene } from '/type/scene'

const {{ camelCase name }} = ({ container }: SceneArgs): RenderScene => {
  return {}
}

export default {{ camelCase name }}